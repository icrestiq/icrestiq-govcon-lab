// Real member testimonials only.
//
// Do NOT add example, placeholder, or illustrative entries here — not even
// commented out. GovCon Lab's own community rules prohibit false claims of
// past performance, and a fabricated quote is exactly that.
//
// Only entries with permissionOnFile: true are ever rendered — the
// Testimonials component filters on that field. Set it to false (or omit
// the entry) until you actually have the member's OK to publish their name,
// company, and quote.
//
// Shape:
// {
//   quote: string,            // the testimonial text, verbatim from the member
//   memberName: string,
//   company: string,          // optional — omit if the member didn't share one
//   permissionOnFile: boolean,
// }

export const TESTIMONIALS = []
